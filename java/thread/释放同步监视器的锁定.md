#释放同步监视器的锁定

##如下情况会释放同步监视器

当前线程的同步方法或同步代码块执行完成

同步方法或同步代码块中遇到break或return终止了执行

同步方法或同步代码中出现未处理的Error或Exception

程序执行了同步监视器对象的wait方法，则当前线程暂停并释放同步监视器

##如下情况不会释放同步监视器

程序调用sleep或yield方法

程序调用suspend方法挂起线程